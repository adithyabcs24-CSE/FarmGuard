[project]
name = "farmguard"
version = "1.0.0"
description = "FarmGuard AI - Early Crop Problem & Pest Detection Platform"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend.main:app"
